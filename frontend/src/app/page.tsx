import { EncryptedIncrement } from "../components/EncryptedIncrement";

export default function Page() {
  return (
    <main style={{ padding: 32, fontFamily: "system-ui, sans-serif" }}>
      <EncryptedIncrement />
    </main>
  );
}
